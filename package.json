{
  "name": "bo-console-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --test && node --check helper/server.js && node --check extension/newtab.js && node --check extension/lib/settings-panel.js"
  },
  "engines": {
    "node": ">=22"
  }
}
